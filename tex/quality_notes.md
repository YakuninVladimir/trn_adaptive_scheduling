# Что изменено

- Убраны лишние сущности и длинные списки.
- Основная линия сведена к одному объекту: условному распределению оракульного времени остановки.
- Правило остановки отделено от вероятностной модели.
- Пять правил остановки: накопленная вероятность, малая вероятность будущего улучшения, условная интенсивность, квантиль, бюджетное правило.
- Шесть семейств распределений в коде/экспериментах: конечное дискретное, сглаженная разметка по потерям, смесь геометрических, смесь дискретизированных экспоненциальных, степенной спад (`power`), отрицательное биномиальное.
- Текст разнесён по `chapters/*.tex`; литература в `references.bib` (`biblatex` + `bibtex`).
- Результаты ARC-AGI подтянуты из `runs/oracle_sweep_arc_agi/stopping_summary.csv` и гистограммы \(\tau^\star\); сравнение с `oracle_finetune/`.
